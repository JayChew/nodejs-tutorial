const lodash = require('lodash'); // Importing the lodash library
const numbers = [1, 2, 3, 4, 5]; // An array of numbers
const reversed = lodash.reverse(numbers); // Reversing the array using lodash
console.log(reversed); // [5, 4, 3, 2, 1]
